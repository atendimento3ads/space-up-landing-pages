---
deployment:
  tasks:
    - export DEPLOYPATH=/home2/hg3ads37/services.spaceupconstruction.com/
    - /bin/mkdir -p "$DEPLOYPATH"
    - /bin/cp .htaccess index.html styles.css "$DEPLOYPATH"
    - /bin/cp -R epoxy-flooring home-construction siding-finishing "$DEPLOYPATH"
