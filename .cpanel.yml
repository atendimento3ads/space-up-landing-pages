---
deployment:
  tasks:
    - export DEPLOYPATH=/home2/hg3ads37/services.spaceupconstruction.com/
    - /bin/mkdir -p "$DEPLOYPATH"
    - /bin/cp .htaccess index.html styles.css favicon.ico robots.txt sitemap.xml llm.txt llms.txt "$DEPLOYPATH"
    - /bin/cp -R assets epoxy-flooring home-construction siding-finishing "$DEPLOYPATH"
